#include <iostream>

// These will work as class names - but don't do it!
class override{

};

// class if{  // Compiler error - "if" is a keyword

// };


int main(){

  // These will work as variables - but don't do it!
  // double final{3.4};
  // std::cout << "final: " << final << '\n';

  // double override{3.4};
  // std::cout << "override: " << override << '\n';

  return 0;
}
